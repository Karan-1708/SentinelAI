[pytest]
asyncio_mode = auto
testpaths = ingestion models api explainability tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: marks tests requiring the full Docker stack (deselect with -m 'not integration')
addopts = -v --tb=short -m "not integration"
